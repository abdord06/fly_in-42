[project]
name = "fly-in"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=2.3.0",
    "pygame-ce>=2.5.7",
]

[tool.mypy]
exclude = [
    '^venv/',
    '^\\.venv/'
]